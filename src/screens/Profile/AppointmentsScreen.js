import React from 'react';
import { StyleSheet, Text, View, TouchableOpacity } from 'react-native';

export default function AppointmentsScreen() {
    return (
      <Text>This is my appointments screen</Text>
    );
  }